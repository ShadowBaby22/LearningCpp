#include <iostream> 
using namespace std;

enum hello{
    a,b=99,c,d=-1
};

enum color{
    blue,red
};

enum game{
//goop=70, poog=101.0
};

int main () {
        
        //cout<<(5 < 8 ? 1 !=2 < 5==0 ? 10:20:30);

        int test=0;
        cout<<'1';
        cout<< (test ? 3:'1') << '\n'; //149? ASCII Value? 
        

    enum hello m;
    cout <<a << b<< c<< d << '\n';

    cout<< sizeof(color);

    //cout <<poog;

    cout << 'a' + 'I';

    return 0;

}